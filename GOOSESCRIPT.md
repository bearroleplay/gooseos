# 🦢 **GOOSESCRIPT — ДОКУМЕНТАЦИЯ ЯЗЫКА**

Версия: **2.0** (v0.1.0-alpha)  
Совместимость: **GooseOS v0.1.0 и выше**

---

## 📋 **СОДЕРЖАНИЕ**
1. [Введение](#введение)
2. [Переменные](#переменные)
3. [Типы данных](#типы-данных)
4. [Операции](#операции)
5. [Управляющие конструкции](#управляющие-конструкции)
6. [Ввод/вывод](#вводвывод)
7. [Графика](#графика)
8. [Примеры](#примеры)
9. [Ошибки](#ошибки)

---

## **ВВЕДЕНИЕ**

GooseScript — это встроенный язык программирования GooseOS. Он создан для:
- Написания простых программ прямо в системе
- Автоматизации задач
- Создания игр (да, змейка работает!)
- Изучения основ программирования

**Запуск программы:**
```bash
edit имя_файла.goo   # Создать/редактировать
compile имя_файла.goo # Скомпилировать
run имя_файла.goobin  # Запустить
```

Или прямо из редактора: `Ctrl+R`

---

## **ПЕРЕМЕННЫЕ**

### Объявление
```goo
let имя = значение
```

### Присваивание
```goo
имя = новое_значение
```

### Примеры
```goo
let x = 10              // число
let name = "Goose"      // строка
let y = x               // копирование переменной
x = x + 5               // изменение
```

**Важно:** Все переменные глобальные, типы меняются динамически.

---

## **ТИПЫ ДАННЫХ**

| Тип | Описание | Пример |
|-----|----------|--------|
| **Число** | Целые числа (32 бита) | `42`, `-7`, `0` |
| **Строка** | Текст в кавычках | `"Hello"` |
| **Логический** | Результат сравнений | `true`, `false` (пока только внутри условий) |

```goo
let a = 42          // число
let b = "text"      // строка
let c = a           // число
b = 100             // теперь b - число!
```

---

## **ОПЕРАЦИИ**

### Арифметика
```goo
x + y    // сложение
x - y    // вычитание
x * y    // умножение
x / y    // деление
```

Пример:
```goo
let a = 10
let b = 3
let c = a + b    // 13
c = c * 2        // 26
```

### Сравнения
```goo
x == y    // равно
x < y     // меньше
x > y     // больше
```

Используются в `if` и `while`:
```goo
if x == 10 { ... }
while i < 5 { ... }
```

---

## **УПРАВЛЯЮЩИЕ КОНСТРУКЦИИ**

### Условие if/else
```goo
if условие {
    // код если истина
} else {
    // код если ложь
}
```

**Пример:**
```goo
let age = 18
if age >= 18 {
    print "Взрослый"
} else {
    print "Ребенок"
}
```

### Цикл while
```goo
while условие {
    // повторяющийся код
}
```

**Пример:**
```goo
let i = 0
while i < 5 {
    print_num i
    i = i + 1
}
// Вывод: 0 1 2 3 4
```

---

## **ВВОД/ВЫВОД**

### Вывод
```goo
print "текст"        // вывод строки
print_num переменная // вывод числа
println "текст"      // вывод строки с переводом строки
```

**Пример:**
```goo
let x = 42
print "x = "
print_num x
println "!"          // после числа будет перевод строки
```

### Ввод с клавиатуры
```goo
input "приглашение", переменная
```

**Пример:**
```goo
input "Как тебя зовут? ", name
print "Привет, "
print name
println "!"
```

Если ввести число — сохранится как число, иначе как строка.

---

## **ГРАФИКА**

### Очистка экрана
```goo
clear
```

### Установка курсора
```goo
gotoxy x, y
```
Где `x` (0-79), `y` (0-24)

### Вывод символа
```goo
putchar x, y, 'символ'
```

### Рисование прямоугольника
```goo
draw_rect x, y, ширина, высота, 'символ'
```

**Пример:**
```goo
clear
draw_rect 10, 5, 20, 10, '#'
putchar 15, 8, '@'
gotoxy 0, 20
print "Графика работает!"
```

---

## **ПРИМЕРЫ ПРОГРАММ**

### Пример 1: Калькулятор
```goo
input "Введи первое число: ", a
input "Введи второе число: ", b
let c = a + b
print a
print " + "
print b
print " = "
print_num c
println ""
```

### Пример 2: Счётчик
```goo
let i = 1
while i <= 10 {
    print_num i
    print " "
    i = i + 1
}
println ""
```

### Пример 3: Угадай число
```goo
let secret = 42
let guess = 0
let attempts = 0

println "Угадай число (1-100)!"

while guess != secret {
    input "Твой вариант: ", guess
    attempts = attempts + 1
    
    if guess < secret {
        println "Больше!"
    } else {
        if guess > secret {
            println "Меньше!"
        }
    }
}

print "Угадал за "
print_num attempts
println " попыток!"
```

### Пример 4: Змейка (упрощённо)
```goo
let x = 40
let y = 12
let dir = 1
let score = 0
let food_x = 20
let food_y = 10

clear
draw_rect 0, 0, 79, 24, '#'

while 1 {
    putchar food_x, food_y, '*'
    putchar x, y, 'O'
    
    input "", key
    if key == 'w' { y = y - 1 }
    if key == 's' { y = y + 1 }
    if key == 'a' { x = x - 1 }
    if key == 'd' { x = x + 1 }
    if key == 'q' { break }
    
    if x == food_x && y == food_y {
        score = score + 1
        food_x = food_x + 5
        food_y = food_y + 2
    }
    
    gotoxy 2, 1
    print "Score: "
    print_num score
}
clear
println "Game Over!"
```

---

## ⚠️ **ОШИБКИ**

### Возможные ошибки:
| Ошибка | Причина |
|--------|---------|
| `Compilation failed` | Синтаксическая ошибка в коде |
| `VM creation failed` | Нет памяти |
| `Unknown instruction` | Байт-код повреждён |

### Отладка:
1. Проверьте синтаксис (скобки, кавычки)
2. Убедитесь, что переменные объявлены через `let`
3. Используйте `print` для отслеживания значений

---

## **СОВЕТЫ**

1. **Всегда объявляйте переменные** через `let` до использования
2. **Не забывайте кавычки** для строк
3. **Ставьте пробелы** для читаемости
4. **Используйте отступы** в блоках `{}` (они не обязательны, но помогают)
5. **Проверяйте границы** при работе с графикой (x 0-79, y 0-24)

---

## **ЧТО ДАЛЬШЕ?**

В следующих версиях GooseOS:
- Функции (`fn add(a,b) { return a+b }`)
- Массивы
- Работа с файлами
- Настоящие игры
